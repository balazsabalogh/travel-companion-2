# RC8 · Developer QA

## Static/regression pass
39 / 39 checks passed.

Covered version/cache consistency, requested RC8 features, route-day gesture removal, explicit day controls, route handle geometry, compact/full Days geometry, Sonar controls, notification reset handling, service-worker asset completeness, image decoding/fallbacks and core-function presence.

The following navigation functions were compared directly against RC7 and were unchanged: `transitUrl`, `openExternalTransit`, `showNavigationChoice`, `startNavigation`, `drawNavigationRoute`, `renderNavBanner`, `nextNavigationTarget`, `hideNavigationUI`, `endNavigation`, `navigationMain`.

## Mobile UI/gesture pass · 414 × 896
21 / 21 checks passed.

Covered compact Days alignment/readability, equal full-day geometry including Day 4, explicit route day controls, route handle tap, short/long logo behavior, Sonar controls/radius row, notification cancel reset, route image fallbacks and runtime errors.
